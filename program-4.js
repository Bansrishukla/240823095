// Program to calculate factorial of a number

// Input number
let num = 4;
let factorial = 1;

// Calculate factorial using a loop
for (let i = 1; i <= num; i++) {
    factorial *= i;
}

// Output the result
console.log(factorial);
